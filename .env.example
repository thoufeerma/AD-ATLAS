# Copy to .env.local.
# Where the Velastia API runs. Read on the server only: pages fetch from it
# directly, and the browser reaches it through the /api/v1 rewrite in
# next.config.ts, so the API never needs to accept cross-origin requests.
# The API must be running for `next build` too — pages are pre-rendered from it.
API_URL=http://localhost:4000
